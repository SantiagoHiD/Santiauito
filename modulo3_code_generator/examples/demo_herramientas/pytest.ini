[pytest]
# Registrar el marker 'scenario' evita el warning de pytest y documenta
# para que sirve: vincular un test con un escenario del Contract B.
markers =
    scenario: vincula un test con un escenario del Contract B (trazabilidad CMMI L3)
